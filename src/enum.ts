enum ButtonType {
    KAKAO_BUTTON,
    GOOGLE_BUTTON,
    PRIMARY_BUTTON,
    STUDY_CREATE_BTTUN,
}

enum LinkType {
    PRIMARY_LINK,
    EMAIL_RESAND_LINK,
    HEADER_LINK,
}

enum ValidationType {
    NORMAL,
    VALIDATE,
    NON_VALIDATE
}

enum ImageType {
    LOGO_IMG,
}

export { ButtonType, LinkType, ValidationType, ImageType };